class Solution {
    public int solution(String s) {
        int answer = 0;
        answer = Integer.parseInt(s);
        return answer;
    }
}

// Integer.parseInt(s);
// 숫자형의 문자열을 인자값으로 받으면
// 해당값을 10진수의 Integer 값으로 반환